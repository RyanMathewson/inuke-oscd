# Normalize text files to LF in the repo; git converts on checkout per-platform.
* text=auto eol=lf

# Explicit binaries -- never touch line endings on these.
*.png binary
*.pcap binary
*.pdf binary
*.exe binary
